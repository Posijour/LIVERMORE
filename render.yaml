services:
  - type: web
    name: livermore-core-bot
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python run_tg.py
    healthCheckPath: /health
    autoDeploy: true
